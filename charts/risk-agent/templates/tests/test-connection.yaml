apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "risk-agent.fullname" . }}-test-connection"
  labels:
    {{- include "risk-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "risk-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
