apiVersion: v1
kind: Service
metadata:
  name: risk-agent
  labels:
    app: risk-agent
spec:
  selector:
    app: risk-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP