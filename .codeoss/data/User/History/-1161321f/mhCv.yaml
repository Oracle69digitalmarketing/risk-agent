kind: Service
metadata:
  name: risk-agent-service
  labels:
    app: risk-agent
spec:
  selector:
    app: risk-agent
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: http
  type: ClusterIP

